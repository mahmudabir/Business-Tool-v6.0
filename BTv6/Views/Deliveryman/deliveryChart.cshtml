@{
    var myChart = new Chart(width: 600, height: 400)
    .AddTitle("Delivery Chart")
    .AddSeries(
    name: "Delivery Accepted",
    xValue: new[] { "Deliveryman" },
    yValues: new[] { ViewData["delivery"] })
    .Write();
}

